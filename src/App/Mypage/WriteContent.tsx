import { Container } from "../../components/common";
import { Text } from "react-native";

const WriteContent: React.FC = () => {
  return (
    <Container>
      <Text style={{ backgroundColor: "skyblue" }}> 작성한 댓글 목록 페이지 </Text>
    </Container>
  );
};

export default WriteContent;
